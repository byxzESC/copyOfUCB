//WRITE YOUR CODE BELOW

var hidden = "";

var key = {
    name: "alex",
    date: 12/20/2022,
    lastExchange: false,
    keyID: {
        asd: AudioDestinationNode,
        sdf: {
            msglk: AudioDestinationNode,
            number: [0, 1, 2, hidden]
        }
    }
};


console.log("Customer " + customerOrder.name + " would like " + customerOrder.sugar + " sugar cubes.");

if (customerOrder.isReady === true) {
    console.log("Ready for pick-up");
} else {
    console.log("Still in order queue");
}